name: veil-env
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - pip:
      - pyside6>=6.6
      - pillow>=10.0
      - numpy>=1.24
      - pyyaml>=6.0
      - rich>=13.0
      - requests>=2.31
      - watchdog>=3.0
      - uvicorn>=0.29
      - fastapi>=0.110
      - black
      - ruff
      - pytest
      - pytest-cov
